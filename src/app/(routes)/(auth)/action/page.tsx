import React from 'react';

const page = () => {
    // will grab the url and based on the mode action will be decided and will verify user using jtw which will be sent via email
    return (
        <div>
            {/* action for reset password, forget email, verify email */}
        </div>
    );
};

export default page;